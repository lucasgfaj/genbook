<div class="container mt-5">
    <h2 class="fw-bold mb-4">Adicionar Novo Livro</h2>

    <form id="addBookForm" method="POST" action="/books" enctype="multipart/form-data">

        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="bookTitle" class="form-label">Título</label>
                <input
                    type="text"
                    class="form-control"
                    id="bookTitle"
                    name="book[title]"
                    placeholder="Título do livro"
                    required
                    value="<?= htmlspecialchars($book->title ?? '') ?>">
            </div>

            <div class="col-md-6">
                <label class="form-label">Autores</label>
                <select name="authors[]" class="form-select" multiple required>
                    <?php foreach ($authors as $author): ?>
                        <option value="<?= $author->id ?>"
                            <?= in_array($author->id, array_map(fn($a) => $a->id, $book->book_authors)) ? 'selected' : '' ?>>
                            <?= $author->full_name ?>
                        </option>
                    <?php endforeach; ?>
                </select>
                <small class="text-muted">Segure Ctrl (ou Cmd) para selecionar múltiplos autores</small>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <label class="form-label">Categoria</label>
                <select name="category_id" class="form-select" required>
                    <option value="">Selecione uma categoria</option>
                    <?php foreach ($categories as $category): ?>
                        <option value="<?= htmlspecialchars($book->category_id ?? '') ?>"> <?= $book->category_id == $category->id ? 'selected' : '' ?>>
                            <?= $category->name ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>

            <div class="col-md-4 mb-3">
                <label for="bookYear" class="form-label">Ano de Publicação</label>
                <input
                    type="number"
                    class="form-control"
                    id="bookYear"
                    name="book[year]"
                    placeholder="Ex: 2025"
                    min="0"
                    required
                    value="<?= htmlspecialchars($book->year ?? '2025') ?>">
            </div>

            <div class="col-md-4 mb-3">
                <label for="bookISBN" class="form-label">ISBN</label>
                <input
                    type="text"
                    class="form-control"
                    id="bookISBN"
                    name="book[isbn]"
                    placeholder="ISBN do livro"
                    required
                    value="<?= htmlspecialchars($book->isbn ?? '') ?>">
            </div>
            <div class="col-md-4 mb-2" id="validityDateContainer">
                <label for="validityDate" class="form-label">Data de Validade</label>
                <input
                    type="date"
                    class="form-control form-control-sm"
                    id="validityDate"
                    name="book[validity_date]"
                    value="<?= htmlspecialchars($book->validity_date ?? '') ?>">
            </div>
        </div>

        <div class="row">
            <div class="col-md-4 mb-3">
                <label for="bookEdition" class="form-label">Edição</label>
                <input
                    type="text"
                    class="form-control"
                    id="bookEdition"
                    name="book[edition]"
                    placeholder="Edição do livro"
                    required
                    value="<?= htmlspecialchars($book->edition ?? '2020') ?>">
            </div>

            <div class="col-md-4 mb-3">
                <label for="bookPublisher" class="form-label">Editora</label>
                <input
                    type="text"
                    class="form-control"
                    id="bookPublisher"
                    name="book[publisher]"
                    placeholder="Nome da editora"
                    required
                    value="<?= htmlspecialchars($book->publisher ?? '') ?>">
            </div>

            <div class="col-md-4 mb-3">
                <label for="bookShelf" class="form-label">Local na Estante</label>
                <input
                    type="text"
                    class="form-control"
                    id="bookShelf"
                    name="book[shelf_location]"
                    placeholder="Ex: A3-2"
                    required
                    value="<?= htmlspecialchars($book->shelf_location ?? '') ?>">
            </div>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="bookQuantity" class="form-label">Quantidade</label>
                <input
                    type="number"
                    class="form-control"
                    id="bookQuantity"
                    name="book[quantity]"
                    min="1"
                    required
                    value="<?= htmlspecialchars($book->quantity ?? '1') ?>">
            </div>

            <div class="col-md-6 mb-3">
                <label for="bookImage" class="form-label">Imagem do Livro</label>
                <input
                    type="file"
                    class="form-control"
                    id="bookImage"
                    name="image"
                    accept="image/*">
            </div>
        </div>
        <a href="/books" class="btn btn-secondary me-2 mt-3">Cancelar</a>
        <button type="submit" class="btn btn-primary mt-3">Adicionar Livro</button>
    </form>
</div>