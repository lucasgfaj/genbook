<div class="container mt-5 mb-5">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
        <h2 class="fw-bold mb-0"><i class="bi bi-pencil-square me-2"></i>Editar Livro</h2>
        <a href="/books" class="btn btn-outline-secondary d-flex align-items-center">
            <i class="bi bi-arrow-left me-1"></i> Voltar
        </a>
    </div>

    <form method="POST" enctype="multipart/form-data" action="<?= route('books.update', ['id' => $book->id]) ?>">
        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label">Título</label>
                <input type="text" name="title" class="form-control" value="<?= $book->title ?>" required>
            </div>

            <div class="col-md-6">
                <label class="form-label">Autores</label>
                <select name="authors[]" class="form-select" multiple required>
                    <?php foreach ($authors as $author): ?>
                        <option value="<?= $author->id ?>"
                            <?= in_array($author->id, array_map(fn($a) => $a->id, $book->book_authors)) ? 'selected' : '' ?>>
                            <?= $author->full_name ?>
                        </option>
                    <?php endforeach; ?>
                </select>
                <small class="text-muted">Segure Ctrl (ou Cmd) para selecionar múltiplos autores</small>
            </div>

            <div class="col-md-6">
                <label class="form-label">Editora</label>
                <input type="text" name="publisher" class="form-control" value="<?= $book->publisher ?>" required>
            </div>

            <div class="col-md-6">
                <label class="form-label">ISBN</label>
                <input type="text" name="isbn" class="form-control" value="<?= $book->isbn ?>" required>
            </div>

            <div class="col-md-4">
                <label class="form-label">Edição</label>
                <input type="text" name="edition" class="form-control" value="<?= $book->edition ?>" required>
            </div>

            <div class="col-md-4">
                <label class="form-label">Ano</label>
                <input type="number" name="year" class="form-control" value="<?= $book->year ?>" required>
            </div>

            <div class="col-md-4">
                <label class="form-label">Quantidade</label>
                <input type="number" name="quantity" class="form-control" value="<?= $book->quantity ?>" required>
            </div>

            <div class="col-md-6">
                <label class="form-label">Localização na Estante</label>
                <input type="text" name="shelf_location" class="form-control" value="<?= $book->shelf_location ?>" required>
            </div>

            <div class="col-md-6">
                <label class="form-label">Status</label>
                <select name="is_active" class="form-select">
                    <option value="1" <?= $book->is_active ? 'selected' : '' ?>>Ativo</option>
                    <option value="0" <?= !$book->is_active ? 'selected' : '' ?>>Inativo</option>
                </select>
            </div>

            <div class="col-md-6">
                <label class="form-label">Capa do Livro (opcional)</label>
                <input type="file" name="cover_name" class="form-control">
            </div>

            <div class="col-md-6">
                <label class="form-label">Capa Atual</label><br>
                <img src="<?= $book->cover_name ?>" alt="Capa do Livro" class="img-thumbnail" style="max-height: 200px;">
            </div>
        </div>

        <div class="d-flex justify-content-end mt-4 gap-2">
            <button type="submit" class="btn btn-success">
                <i class="bi bi-check-circle"></i> Salvar Alterações
            </button>
        </div>
    </form>
</div>