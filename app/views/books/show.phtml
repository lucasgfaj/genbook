<div class="container mt-5 mb-5">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
        <h2 class="fw-bold mb-0"><i class="bi bi-book me-2"></i>Detalhes do Livro</h2>
        <a href="/books" class="btn btn-outline-secondary d-flex align-items-center">
            <i class="bi bi-arrow-left me-1"></i> Voltar
        </a>
    </div>

    <form class="shadow-lg p-4 rounded bg-white border">
        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label">Título</label>
                <input type="text" class="form-control" value="<?= $book->title ?>" disabled>
            </div>
            <?php foreach ($book->book_authors as $author): ?>
                <div class="col-md-6">
                <label class="form-label">Autor(a)</label>
                    <input type="text" class="form-control" value="<?= $author->full_name ?>" disabled>
                </div>
            <?php endforeach; ?>
            

            <div class="col-md-6">
                <label class="form-label">Editora</label>
                <input type="text" class="form-control" value="<?= $book->publisher ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">ISBN</label>
                <input type="text" class="form-control" value="<?= $book->isbn ?>" disabled>
            </div>

            <div class="col-md-4">
                <label class="form-label">Edição</label>
                <input type="text" class="form-control" value="<?= $book->edition ?>" disabled>
            </div>

            <div class="col-md-4">
                <label class="form-label">Ano</label>
                <input type="number" class="form-control" value="<?= $book->year ?>" disabled>
            </div>

            <div class="col-md-4">
                <label class="form-label">Quantidade</label>
                <input type="number" class="form-control" value="<?= $book->quantity ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Localização na Estante</label>
                <input type="text" class="form-control" value="<?= $book->shelf_location ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Status</label>
                <input type="text" class="form-control" value="<?= $book->is_active ? 'Ativo' : 'Inativo' ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Cadastrado em</label>
                <input type="text" class="form-control" value="<?= date('d/m/Y H:i', strtotime($book->created_at)) ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Capa do Livro</label><br>
                <img src="<?= $book->cover_name ?>" alt="Capa do Livro" class="img-thumbnail" style="max-height: 200px;">
            </div>
        </div>

        <div class="d-flex justify-content-end mt-4 gap-2">
            <a href="/books/edit/<?= $book->id ?>" class="btn btn-primary">
                <i class="bi bi-pencil-square"></i> Editar
            </a>
            <?php if (!empty($user['admin'])): ?>
                <a href="/books/delete/<?= $book->id ?>" class="btn btn-warning">
                    <i class="bi bi-trash"></i> Inativar
                </a>
                <a href="/books/destroy/<?= $book->id ?>" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir permanentemente este livro?')">
                    <i class="bi bi-exclamation-triangle-fill"></i> Excluir permanentemente
                </a>
            <?php else: ?>
                <a href="/books/delete/<?= $book->id ?>" class="btn btn-danger">
                <i class="bi bi-trash"></i> Deletar
            </a>
            <?php endif; ?>            
        </div>
    </form>
</div>