<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-4 gap-2 flex-column flex-md-row">

    <!-- Título -->
    <h2 class="fw-bold m-0 text-center text-md-start w-100 w-md-auto">Categorias</h2>

    <!-- Grupo de busca e botões -->
    <div class="d-flex align-items-stretch flex-wrap gap-2 w-100 justify-content-center justify-content-md-end">
      <!-- Campo de busca -->
      <div class="input-group shadow-sm rounded" style="min-width: 250px; max-width: 300px;">
        <span class="input-group-text bg-white border-end-0">
          <i class="bi bi-search text-muted"></i>
        </span>
        <input type="text" class="form-control border-start-0" placeholder="Buscar categorias...">
      </div>

      <!-- Botão de buscar -->
      <button class="btn btn-primary d-flex align-items-center gap-2 shadow-sm">
        <i class="bi bi-search"></i>
        Buscar
      </button>

      <!-- Botão de adicionar -->
      <button type="button" class="btn btn-warning d-flex align-items-center gap-2 shadow-sm" data-bs-toggle="modal"
        data-bs-target="#addCategoryModal">
        <i class="bi bi-folder-plus"></i>
        Adicionar Categoria
      </button>

    </div>
  </div>

  <div class="table-responsive shadow-lg rounded-3 border border-light bg-white p-3">
    <table class="table table-bordered table-hover align-middle mb-0 bg-white">
      <thead class="table-light">
        <tr>
          <th><i class="bi bi-tag-fill me-1"></i>Nome</th>
          <th><i class="bi bi-card-text me-1"></i>Descrição</th>
          <th><i class="bi bi-gear-fill me-1"></i>Ações</th>
        </tr>
      </thead>
      <tbody>
        <?php foreach ($categories as $category): ?>
        <tr>
          <td><?= htmlspecialchars($category->name) ?></td>
          <td><?= nl2br(htmlspecialchars($category->description)) ?></td>
          <td>
            <a href="/categories/edit/<?= $category->id ?>" class="btn btn-sm btn-outline-primary d-inline-flex align-items-center gap-1">
              <i class="bi bi-pencil-square"></i> Editar
            </a>
            <a href="#" class="btn btn-sm btn-outline-danger d-inline-flex align-items-center gap-1 ms-2"
               onclick="return confirm('Tem certeza que deseja apagar esta categoria?');">
              <i class="bi bi-trash"></i> Apagar
            </a>
          </td>
        </tr>
        <?php endforeach; ?>
      </tbody>
    </table>
  </div>
</div>

<!-- Modal para adicionar Categoria -->
<div class="modal fade" id="addCategoryModal" tabindex="-1" aria-labelledby="addCategoryModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">

      <div class="modal-header">
        <h5 class="modal-title" id="addCategoryModalLabel">Adicionar Nova Categoria</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
      </div>

      <div class="modal-body">
        <form id="addCategoryForm" method="POST" action="/categories/create">

          <div class="mb-3">
            <label for="categoryName" class="form-label">Nome</label>
            <input type="text" class="form-control" id="categoryName" name="name" placeholder="Nome da categoria" required>
          </div>

          <div class="mb-3">
            <label for="categoryDescription" class="form-label">Descrição</label>
            <textarea class="form-control" id="categoryDescription" name="description" placeholder="Descrição da categoria" rows="4" required></textarea>
          </div>

        </form>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="submit" form="addCategoryForm" class="btn btn-primary">Adicionar Categoria</button>
      </div>

    </div>
  </div>
</div>
