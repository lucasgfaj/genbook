<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-4 gap-2 flex-column flex-md-row">

    <!-- Título -->
    <h2 class="fw-bold m-0 text-center text-md-start w-100 w-md-auto">Autores</h2>

    <!-- Grupo de busca e botões -->
    <div class="d-flex align-items-stretch flex-wrap gap-2 w-100 justify-content-center justify-content-md-end">
      <!-- Campo de busca -->
      <div class="input-group shadow-sm rounded" style="min-width: 250px; max-width: 300px;">
        <span class="input-group-text bg-white border-end-0">
          <i class="bi bi-search text-muted"></i>
        </span>
        <input type="text" class="form-control border-start-0" placeholder="Buscar autores...">
      </div>

      <!-- Botão de buscar -->
      <button class="btn btn-primary d-flex align-items-center gap-2 shadow-sm">
        <i class="bi bi-search"></i>
        Buscar
      </button>

      <!-- Botão para página de adicionar autor -->
      <a href="/authors/new" class="btn btn-warning d-flex align-items-center gap-2 shadow-sm">
        <i class="bi bi-person-plus"></i>
        Adicionar Autor
      </a>

    </div>
  </div>

  <div class="table-responsive shadow-lg rounded-3 border border-light bg-white p-3">
    <table class="table table-bordered table-hover align-middle mb-0 bg-white">
      <thead class="table-light">
        <tr>
          <th><i class="bi bi-person-fill me-1"></i>Nome Completo</th>
          <th><i class="bi bi-flag-fill me-1"></i>Nacionalidade</th>
          <th><i class="bi bi-calendar3 me-1"></i>Data de Nascimento</th>
          <!-- Biografia removida -->
          <th><i class="bi bi-gear-fill me-1"></i>Ações</th>
        </tr>
      </thead>
      <tbody>
        <?php foreach ($authors as $author): ?>
          <tr>
            <td><?= htmlspecialchars($author->full_name) ?></td>
            <td><?= htmlspecialchars($author->nationality) ?></td>
            <td><?= htmlspecialchars($author->birth_date) ?></td>
            <!-- Biografia removida -->
            <td>
              <a href="/authors/show/<?= $author->id ?> class="btn btn-sm btn-outline-primary d-inline-flex align-items-center gap-1">
                <i class="bi bi-pencil-square"></i> Editar
              </a>
            </td>
          </tr>
        <?php endforeach; ?>
      </tbody>
    </table>

    <!-- PAGINATOR -->
    <nav aria-label="Page navigation" class="mt-4">
      <ul class="pagination justify-content-end mb-0">

        <?php if ($paginator->hasPreviousPage()) : ?>
          <li class="page-item">
            <a class="page-link" href="<?= route($paginator->getRouteName(), ['page' => $paginator->previousPage()]) ?>" aria-label="Anterior">
              <span aria-hidden="true">&laquo; Anterior</span>
            </a>
          </li>
        <?php else : ?>
          <li class="page-item disabled" aria-disabled="true">
            <span class="page-link">&laquo; Anterior</span>
          </li>
        <?php endif; ?>

        <?php for ($page = 1; $page <= $paginator->totalOfPages(); $page++) : ?>
          <li class="page-item <?= $paginator->isPage($page) ? 'active' : '' ?>" aria-current="<?= $paginator->isPage($page) ? 'page' : '' ?>">
            <a class="page-link" href="<?= route($paginator->getRouteName(), ['page' => $page]) ?>"><?= $page ?></a>
          </li>
        <?php endfor; ?>

        <?php if ($paginator->hasNextPage()) : ?>
          <li class="page-item">
            <a class="page-link" href="<?= route($paginator->getRouteName(), ['page' => $paginator->nextPage()]) ?>" aria-label="Próxima">
              <span aria-hidden="true">Próxima &raquo;</span>
            </a>
          </li>
        <?php else : ?>
          <li class="page-item disabled" aria-disabled="true">
            <span class="page-link">Próxima &raquo;</span>
          </li>
        <?php endif; ?>

      </ul>
    </nav>

    <div class="d-flex justify-content-end">
      <small class="text-muted"><?= $paginator->entriesInfo() ?></small>
    </div>
  </div>
</div>
