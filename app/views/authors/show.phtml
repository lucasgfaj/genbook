<div class="container mt-5 mb-5">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
        <h2 class="fw-bold mb-0"><i class="bi bi-person-fill me-2"></i>Detalhes do Autor</h2>
        <a href="/authors" class="btn btn-outline-secondary d-flex align-items-center">
            <i class="bi bi-arrow-left me-1"></i> Voltar
        </a>
    </div>

    <form class="shadow-lg p-4 rounded bg-white border">
        <div class="row g-3">

            <div class="col-md-6">
                <label class="form-label">Nome Completo</label>
                <input type="text" class="form-control" value="<?= htmlspecialchars($author->full_name) ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Nacionalidade</label>
                <input type="text" class="form-control" value="<?= htmlspecialchars($author->nationality) ?>" disabled>
            </div>

            <div class="col-md-6">
                <label class="form-label">Data de Nascimento</label>
                <input type="text" class="form-control" value="<?= htmlspecialchars(date('d/m/Y', strtotime($author->birth_date))) ?>" disabled>
            </div>

            <div class="col-md-12">
                <label class="form-label">Biografia</label>
                <textarea class="form-control" rows="5" disabled><?= htmlspecialchars($author->bio) ?></textarea>
            </div>

            <div class="col-md-6">
                <label class="form-label">Cadastrado em</label>
                <input type="text" class="form-control" value="<?= htmlspecialchars(date('d/m/Y H:i', strtotime($author->created_at))) ?>" disabled>
            </div>

        </div>

        <div class="d-flex justify-content-end mt-4 gap-2">
            <a href="/authors/edit/<?= $author->id ?>" class="btn btn-primary">
                <i class="bi bi-pencil-square"></i> Editar
            </a>
            <?php if (!empty($user['admin'])): ?>
                <a href="/authors/delete/<?= $author->id ?>" class="btn btn-warning">
                    <i class="bi bi-trash"></i> Inativar
                </a>
                <a href="/authors/destroy/<?= $author->id ?>" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir permanentemente este autor?')">
                    <i class="bi bi-exclamation-triangle-fill"></i> Excluir permanentemente
                </a>
            <?php else: ?>
                <a href="/authors/delete/<?= $author->id ?>" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Deletar
                </a>
            <?php endif; ?>
        </div>
    </form>
</div>
